## Featured Products
Question 1: Marking a product as featured in the admin interface causes the `is_featured` field of the Product model to be set to "True". The `products/views.py` file passes the data from the model to `products/catalog.html` in order to render the catalog page. Similarly, the view passes data from the model to `products/detail.html` in order to render a product's detail page. In the `catalog.html` file, a for loop is used to run through and display every product in the model. Inside this loop there is an if statement that checks to see if `is_featured` is set to "True". If it is, then the "Featured" badge is displayed next to the product's title. The code in `detail.html` is a little simpler, as there is no loop involved. After displaying the product's title, the code simply checks to see if `is_featured` is true, and displays the badge if it is.

Question 2: I verified the added feature by running the server and checking the catalog to see if the three specified items displayed the "Featured" badge. I also looked to see if any other product incorrectly displayed the badge. For each item, I navigated to the product detail page and made sure that the badge appeared there as well. I then went back to the catalog page and navigated to the product detail page for several products that were not featured, ensuring that the badge did not incorrectly display on a detail page.

Question 3: I was surprised when the agent initially made the "Featured" badge the exact same color as the "Add to Cart" button. The badge and the button end up appearing close together on screen, so the visual clarity is needed to differentiate the two. I simply asked the agent to change the color of the "Featured" badge, and explained my reasoning behind the request. Not only did the agent make the change, but it also admitted its mistake in color choice. Another thing that I did not expect, although in hind sight it makes sense, is the fact that the agent was unaware of any changes I made through Django's admin interface. In its final summary to `PROMPTS.md`, the agent mentions that no products have been flagged as featured, and therefore no confirmation has been made even though I made the changes and verified them myself by running the server. In a similar vein, I ran the tailwind build command it suggested in my second terminal, and it mentioned in the summary that I ignored its suggestion. Would the best solution be to have the agent execute these commands for me, or ask it to re-read the project files again so that it sees the updates?